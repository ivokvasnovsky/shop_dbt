version: 2

seeds:
  - name: dim_countries
    columns:
      - name: alpha-2
        tests:
          - unique
          - not_null
      - name: country-code
        tests:
          - unique
          - not_null

  - name: dim_calendar
    columns:
      - name: date
        tests:
          - unique
          - not_null