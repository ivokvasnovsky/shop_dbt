version: 2

seeds:
  - name: dim_countries
    columns:
      - name: "alpha-2"
        quote: true  # <--- Add this
        tests:
          - unique
          - not_null
      - name: "country-code"
        quote: true  # <--- Add this
        tests:
          - unique
          - not_null

  - name: dim_calendar
    columns:
      - name: date
        tests:
          - unique
          - not_null